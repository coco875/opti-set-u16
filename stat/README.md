list of graph:

* a graph comparing the avg time of all SetInt implementation
* a graph per scenario that compare the avg time of all SetInt implementation
* a graph per scenario that compare the avg time of all SetInt impl for each maximum capacity
